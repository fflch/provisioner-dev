---
- name: deploy vagrantpythonweb
  become: yes
  hosts: vagrantpythonweb

  tasks:

    - name: Aplica role fflch.tasks 
      include_role:
        name: fflch.tasks
      vars:
        tasks_django: true
        tasks_django_user_password: SuperSenha123
